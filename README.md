# Basola Website

## Need Section

Die Komponente `NeedSection.astro` befindet sich unter `src/components` und bindet automatisch das Skript `src/scripts/need-animate.ts` ein. Füge sie auf einer Seite mit:

```astro
---
import NeedSection from '@/components/NeedSection.astro';
---
<NeedSection />
```

Weitere Tailwind-Plugins sind nicht erforderlich.
